use python3.7 preferrable
1) create venv:
    python -m venv venv
    
2) activate it
    source venv/bin/activate
    
3) install modules
    pip install -r requirements.txt

4) run django server (u can use another port 127.0.0.1:8001 for example)
    python manage.py runserver 127.0.0.1:8000
 
